// Token generation utility — authentication uses Firebase ID tokens verified
// via admin.auth().verifyIdToken() in middleware/authMiddleware.js.
// JWT generation is therefore handled by Firebase; this file is not needed
// but is kept as a placeholder in case custom token logic is added later.
