// Database config — this project uses Firebase Firestore via the Admin SDK.
// The Firestore instance is exported from config/firebase.js as `db`.
// There is no separate SQL/MongoDB connection; this file is kept as a placeholder.
//
// Usage anywhere in the backend:
//   const { db } = require("./config/firebase");
